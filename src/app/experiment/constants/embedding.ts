import { EmbeddingModel } from "@/app/experiment/types/embedding";

export const DEFAULT_QUESTION =
  "What must a lawyer do before citing a case that an AI tool produced?";

export const DEFAULT_MODEL: EmbeddingModel = "Snowflake/snowflake-arctic-embed-xs";
